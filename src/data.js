
import Harry from './data/potter/potter.js';

export const harryp = (type = undefined) => {
  if(!type){
    return Harry;
  }
  //return Harry.filter(p => p.type.map(t => t.toLowerCase()).indexOf(type.toLowerCase()) >= 0);
 
//};









/* Manejo de data */

// esta es una función de ejemplo


  //return 'example';
//};
